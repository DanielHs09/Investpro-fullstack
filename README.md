Badges (React, Node, MongoDB, MIT)
Features list (User + Admin)
Full tech stack table
Step-by-step setup instructions
Demo credentials
Complete API reference tables
Project folder structure
Deployment guide (Render + Netlify)
Security overview
Contributing guide
.gitignore covers:
node_modules/ — both frontend and backend
.env files — all variants so secrets are never pushed
build/ and dist/ — compiled output
OS files — .DS_Store, Thumbs.db
Editor files — .vscode/, .idea/
Logs, test coverage, and misc
